#pragma once
#include"DxLib.h"

//色
namespace color
{
	// 白
	const int WhiteColor = GetColor(255, 255, 255);
	// 黒
	const int BlackColor = GetColor(0, 0, 0);
	// 灰
	const int GrayColor = GetColor(128, 128, 128);
	// 薄灰
	const int LightGrayColor = GetColor(192, 192, 192);
	// 濃灰
	const int DarkGrayColor = GetColor(64, 64, 64);
	// 赤
	const int RedColor = GetColor(255, 0, 0);
	// 緑
	const int GreenColor = GetColor(0, 255, 0);
	// 青
	const int BlueColor = GetColor(0, 0, 255);
	// 黄
	const int YellowColor = GetColor(255, 255, 0);
	// シアン
	const int CyanColor = GetColor(0, 255, 255);
	// マゼンタ
	const int MagentaColor = GetColor(255, 0, 255);
	// オレンジ
	const int OrangeColor = GetColor(255, 128, 0);
	// 紫
	const int PurpleColor = GetColor(128, 0, 128);
	// ピンク
	const int PinkColor = GetColor(255, 20, 147);
	// 濃赤
	const int DarkRedColor = GetColor(128, 0, 0);
	// 濃い緑
	const int DarkGreenColor = GetColor(0, 128, 0);
	// 深藍
	const int NavyColor = GetColor(0, 0, 128);
	// 茶
	const int BrownColor = GetColor(153, 51, 0);
}